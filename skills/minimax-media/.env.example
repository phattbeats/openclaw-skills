# MiniMax credentials. Use the SUBSCRIPTION KEY for Token Plan quota;
# a standard Open Platform API key bills pay-as-you-go against wallet balance.
# Copy to .env and fill in. Never commit; never paste into issues.
MINIMAX_API_KEY=
MINIMAX_BASE_URL=https://api.minimax.io
MINIMAX_OUTPUT_DIR=./output
